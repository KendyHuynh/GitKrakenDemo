@model MVCElectronicStore.Models.LoginViewModel

@{
    ViewBag.Title = "Login";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="text-center">
    <h2>ĐĂNG NHẬP</h2>
</div>
<br />
<div class="row">
    <div class="offset-md-1 col-md-10">
        <form asp-action="Login" method="post" class="login-form">
            @if (!string.IsNullOrEmpty(ViewBag.ErrorMessage))
            {
                <div class="alert alert-danger">@ViewBag.ErrorMessage</div>
            }

            <div class="form-group">
                <label for="dangnhap">Tên đăng nhập:</label>
                <input asp-for="Username" class="form-control" id="dangnhap" required />
            </div>
            <div class="form-group">
                <label for="matkhau">Mật khẩu:</label>
                <input asp-for="Password" class="form-control" id="matkhau" required />
            </div>
            <div class="form-group text-center">
                <button type="submit" class="btn btn-primary">ĐĂNG NHẬP</button>
            </div>
        </form>
    </div>
</div>